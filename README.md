# git-scene :tada:

Git contribution calendar, authors list and commits count

#### Steps to Install

`npm i git-scene -S`

or

`yarn add git-scene`

In package.json, add
`scripts: { "git-scene": "git-scene" }`

### Run
`npm run git-scene`

Thats It !!!!

